<div class="container">
  <div class = "jumbotron col-md-8 offset-md-1">
    <h1 class="text-success text-left">＜記録者登録情報＞</h1>
      <p>お名前：<%= @user.name %>様</p>
      <p>職種：<%= @user.genre_name %></p>

      <% if @user.id == current_user.id %>
        <p><%= link_to "プロフィール編集", edit_user_path(@user), class: "btn btn-success text-white ms-2" %></p>
      <% end %>
      <div class="users-info">
        <%= image_tag '5182522_l.jpg', class: "card-img imag-fluid rounded-0"  %>
      </div>
  </div>
</div>